<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- android:showAsAction="never" -->
    <!-- ifRoom    会显示在Item中，但是如果已经有4个或者4个以上的Item时会隐藏在溢出列表中。当然个数并不仅仅局限于4个， -->
    <!-- 依据屏幕的宽窄而定 -->
    <!-- never    永远不会显示。只会在溢出列表中显示，而且只显示标题，所以在定义item的时候，最好把标题都带上。 -->
    <!-- always    无论是否溢出，总会显示。 -->
    <!-- withText    withText值示意Action bar要显示文本标题。Action bar会尽可能的显示这个标题，但是， -->
    <!-- 如果图标有效并且受到Action bar空间的限制，文本标题有可能显示不全。 -->
    <!-- collapseActionView      声明了这个操作视窗应该被折叠到一个按钮中，当用户选择这个按钮时， -->
    <!-- 这个操作视窗展开。否则，这个操作视窗在默认的情况下是可见的，并且即便在用于不适用的时候，也要占据操作栏的有效空间。 -->

<!--     <item -->
<!--         android:id="@+id/menu_find" -->
<!--         android:orderInCategory="100" -->
<!--         android:showAsAction="never" -->
<!--         android:title="@string/menu_find_str"/> -->
    <!-- android:orderInCategory="100"优先级 -->
    <item
        android:id="@+id/menu_all_contact"
        android:orderInCategory="100"
        android:showAsAction="never"
        android:title="@string/menu_find_all_contact_str"/>
    <item
        android:id="@+id/menu_available_contact"
        android:orderInCategory="100"
        android:showAsAction="never"
        android:title="@string/menu_find_available_contact_str"/>

</menu>